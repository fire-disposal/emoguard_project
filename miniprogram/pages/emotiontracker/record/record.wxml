<view class="page">
  <view class="card">
    <view class="title">情绪量表</view>
    <!-- Q1 -->
    <view class="question">
      <view class="question-title">1. 在过去的几个小时里，您是否觉得心情低落，或者对平常喜欢做的事情提不起兴趣？</view>
      <radio-group data-name="depression" bindchange="handleRadioChange">
        <label class="option" wx:for="{{q1Options}}" wx:key="value">
          <radio value="{{item.value}}" checked="{{depression == item.value}}" />{{item.text}}
        </label>
      </radio-group>
    </view>
    <!-- Q2 -->
    <view class="question">
      <view class="question-title">2. 在过去的几个小时里，您是否感到紧张、焦虑，或者坐立不安？</view>
      <radio-group data-name="anxiety" bindchange="handleRadioChange">
        <label class="option" wx:for="{{q2Options}}" wx:key="value">
          <radio value="{{item.value}}" checked="{{anxiety == item.value}}" />{{item.text}}
        </label>
      </radio-group>
    </view>
    <!-- Q3 -->
    <view class="question">
      <view class="question-title">3. 在过去的几个小时里，您是否觉得没有精力，容易感到疲劳？</view>
      <radio-group data-name="energy" bindchange="handleRadioChange">
        <label class="option" wx:for="{{q3Options}}" wx:key="value">
          <radio value="{{item.value}}" checked="{{energy == item.value}}" />{{item.text}}
        </label>
      </radio-group>
    </view>
    <!-- Q4 -->
    <view class="question">
      <view class="question-title">4. 请回顾今天（或昨晚），您的睡眠情况如何？</view>
      <radio-group data-name="sleep" bindchange="handleRadioChange">
        <label class="option" wx:for="{{q4Options}}" wx:key="value">
          <radio value="{{item.value}}" checked="{{sleep == item.value}}" />{{item.text}}
        </label>
      </radio-group>
    </view>
    <button class="submit-btn" type="primary" loading="{{submitting}}" bindtap="handleSubmit">提交</button>
  </view>
</view>